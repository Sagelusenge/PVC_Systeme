export default function StatCard({ label, value, detail, icon: Icon, tone = "", progress = 70 }) { return <article className="metric-card"><div className="metric-top"><span>{label}</span><span className="metric-icon">{Icon && <Icon />}</span></div><div><div className={`metric-value ${tone}`}>{value}</div><div className="metric-foot">{detail}</div><div className="progress"><span style={{width:`${progress}%`,background:tone === "red" ? "var(--red)" : tone === "orange" ? "var(--orange)" : tone === "green" ? "var(--green)" : "var(--blue-soft)"}} /></div></div></article>; }
