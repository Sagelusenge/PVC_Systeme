import{useEffect,useState}from"react";import{Download,FileBarChart,RefreshCw}from"lucide-react";import{getReport,listReports}from"../services/rapports.api";import Button from"../../../components/common/Button";import Badge from"../../../components/common/Badge";import EmptyState from"../../../components/common/EmptyState";import Loader from"../../../components/common/Loader";
export default function RapportsPage(){const[names,setNames]=useState([]);const[selected,setSelected]=useState("");const[rows,setRows]=useState([]);const[loading,setLoading]=useState(true);const load=async name=>{setLoading(true);try{setRows(await getReport(name))}finally{setLoading(false)}};useEffect(()=>{listReports().then(list=>{setNames(list);setSelected(list[0]||"");if(list[0])load(list[0]);else setLoading(false)})},[]);const columns=rows[0]?Object.keys(rows[0]):[];const exportCsv=()=>{if(!rows.length)return;const csv=[columns.join(";"),...rows.map(r=>columns.map(c=>JSON.stringify(r[c]??"")).join(";"))].join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));a.download=`rapport-${selected}.csv`;a.click();URL.revokeObjectURL(a.href)};return<div className="page"><header className="page-header"><div><span className="eyebrow">Analyse & controle // vues SQL</span><h1>Centre des Rapports</h1><p>Etats commerciaux, industriels, financiers et sociaux.</p></div><div className="page-actions"><Button icon={RefreshCw} onClick={()=>load(selected)}>Actualiser</Button><Button variant="primary" icon={Download} onClick={exportCsv}>Exporter CSV</Button></div></header><div className="toolbar"><div className="tabs">{names.map(name=><button key={name} className={`tab ${selected===name?"active":""}`} onClick={()=>{setSelected(name);load(name)}}>{name}</button>)}</div></div><section className="panel"><div className="panel-head"><div><h2>Rapport : {selected}</h2><p>Extraction directe de la base</p></div><Badge>{rows.length} lignes</Badge></div>{loading?<Loader/>:rows.length?<div className="data-table-wrap"><table className="data-table"><thead><tr>{columns.map(c=><th key={c}>{c.replaceAll("_"," ")}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i}>{columns.map(c=><td key={c} className={typeof row[c]==="number"?"number":""}>{String(row[c]??"")}</td>)}</tr>)}</tbody></table></div>:<EmptyState title="Rapport vide" message="Aucune donnee disponible pour cette vue."/>}</section></div>}
