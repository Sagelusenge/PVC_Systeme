import {get,patch,post,remove} from "../../../services/api";export const listPersonnel=()=>get("/rh/personnel?limit=100");export const listPresences=()=>get("/rh/presences?limit=100");export const listPayroll=()=>get("/rh/paie");export const listPayrollPayments=()=>get("/rh/paie-paiements?limit=100");export const createPersonnel=data=>post("/rh/personnel",data);export const updatePersonnel=(id,data)=>patch(`/rh/personnel/${id}`,data);export const deletePersonnel=id=>remove(`/rh/personnel/${id}`);export const createPresence=data=>post("/rh/presences",data);export const updatePresence=(id,data)=>patch(`/rh/presences/${id}`,data);export const deletePresence=id=>remove(`/rh/presences/${id}`);export const createPayroll=data=>post("/rh/paie",data);export const deletePayroll=id=>remove(`/rh/paie-paiements/${id}`);
