import { useState } from "react";
import { Factory, Gauge, PackageCheck, Plus, TimerReset } from "lucide-react";
import useFetch from "../../../hooks/useFetch"; import { createProduct, createProductionEntry, listProducts, listProductionEntries } from "../services/production.api"; import Button from "../../../components/common/Button"; import Modal from "../../../components/common/Modal"; import Input from "../../../components/common/Input"; import Select from "../../../components/common/Select"; import Table from "../../../components/common/Table"; import Badge from "../../../components/common/Badge"; import Loader from "../../../components/common/Loader"; import StatCard from "../../dashboard/components/StatCard"; import {formatCurrency} from "../../../utils/currency"; import {formatDate} from "../../../utils/dates";
export default function ProductionPage(){const state=useFetch(async()=>({products:await listProducts(),entries:await listProductionEntries()}),[]);const[modal,setModal]=useState(null);const[form,setForm]=useState({});const[error,setError]=useState("");if(state.loading)return <Loader/>;const products=state.data?.products||[];const open=t=>{setModal(t);setError("");setForm(t==="product"?{unite:"PIECE",stock_actuel:0}:{id_produit_fini:products[0]?.id||"",unite_mesure:"PIECE",date_entree:new Date().toISOString().slice(0,16)})};const submit=async()=>{try{if(modal==="product")await createProduct({...form,prix_unitaire:Number(form.prix_unitaire),stock_actuel:Number(form.stock_actuel||0)});else await createProductionEntry({...form,id_produit_fini:Number(form.id_produit_fini),quantite_entree:Number(form.quantite_entree),cout_production_unitaire:Number(form.cout_production_unitaire||0)});setModal(null);state.refresh()}catch(e){setError(e.message)}};const value=products.reduce((s,x)=>s+Number(x.stock_actuel)*Number(x.prix_unitaire),0);const columns=[{key:"code",label:"Code PF",render:v=><span className="mono" style={{color:"var(--blue-soft)"}}>{v}</span>},{key:"libelle",label:"Produit fini",render:(v,r)=><><span className="primary-cell">{v}</span><span className="secondary-cell">Unite: {r.unite}</span></>},{key:"stock_actuel",label:"Stock",numeric:true,render:(v,r)=><strong>{v} {r.unite}</strong>},{key:"prix_unitaire",label:"Prix unitaire",numeric:true,render:formatCurrency},{key:"valeur",label:"Valeur stock",numeric:true,render:(_,r)=>formatCurrency(Number(r.stock_actuel)*Number(r.prix_unitaire))},{key:"etat",label:"Ligne",render:()=> <Badge tone="green">Disponible</Badge>}];return <div className="page"><header className="page-header"><div><span className="eyebrow">Lignes A & B // suivi production</span><h1>Production & Extrusion PVC</h1><p>Produits finis, entrees atelier et capacite de fabrication.</p></div><div className="page-actions"><Button onClick={()=>open("product")} icon={Plus}>Nouveau produit</Button><Button variant="primary" onClick={()=>open("entry")} icon={Factory}>Declarer production</Button></div></header><section className="metrics"><StatCard label="Valeur produits finis" value={formatCurrency(value)} detail="Stock valorise au prix de vente" icon={PackageCheck}/><StatCard label="References PF" value={products.length} detail="Produits actifs" icon={Factory} tone="green"/><StatCard label="Lots produits" value={state.data?.entries?.length||0} detail="Receptions atelier tracees" icon={TimerReset} tone="orange"/><StatCard label="Disponibilite usine" value="ONLINE" detail="API & base connectees" icon={Gauge} tone="green" progress={96}/></section><section className="panel"><div className="panel-head"><div><h2>Registre des produits finis</h2><p>Etat de disponibilite commerciale</p></div><Badge>tproduitfini</Badge></div><Table columns={columns} rows={products}/></section><section className="panel"><div className="panel-head"><div><h2>Dernieres entrees de production</h2><p>Lots receptionnes depuis l'atelier</p></div><Badge>tentrees_pf</Badge></div><Table rows={state.data?.entries||[]} columns={[{key:"id",label:"Lot"},{key:"id_produit_fini",label:"Produit ID"},{key:"date_entree",label:"Date",render:formatDate},{key:"quantite_entree",label:"Quantite",numeric:true},{key:"cout_production_unitaire",label:"Cout U.",numeric:true,render:formatCurrency}]}/></section><Modal open={Boolean(modal)} title={modal==="product"?"Nouveau produit fini":"Nouvelle entree de production"} onClose={()=>setModal(null)} footer={<><Button onClick={()=>setModal(null)}>Annuler</Button><Button variant="primary" onClick={submit}>Enregistrer</Button></>}>{error&&<div className="form-error">{error}</div>}<div className="form-grid" style={{marginTop:12}}>{modal==="product"?<><Input label="Code" value={form.code||""} onChange={e=>setForm({...form,code:e.target.value})}/><Input label="Libelle" value={form.libelle||""} onChange={e=>setForm({...form,libelle:e.target.value})}/><Input label="Unite" value={form.unite||""} onChange={e=>setForm({...form,unite:e.target.value})}/><Input label="Prix unitaire" type="number" value={form.prix_unitaire||""} onChange={e=>setForm({...form,prix_unitaire:e.target.value})}/></>:<><Select label="Produit fini" value={form.id_produit_fini} onChange={e=>setForm({...form,id_produit_fini:e.target.value})} options={products.map(x=>({value:x.id,label:`${x.code} - ${x.libelle}`}))}/><Input label="Quantite produite" type="number" value={form.quantite_entree||""} onChange={e=>setForm({...form,quantite_entree:e.target.value})}/><Select label="Unite" value={form.unite_mesure} onChange={e=>setForm({...form,unite_mesure:e.target.value})} options={["PIECE","KG","Litre"]}/><Input label="Cout unitaire" type="number" value={form.cout_production_unitaire||""} onChange={e=>setForm({...form,cout_production_unitaire:e.target.value})}/></>}</div></Modal></div>}
